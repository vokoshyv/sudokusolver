var solveThePuzzle = function(){
  var work = [];
  var addTimes = 9;

  // creates empty matrix
  while (addTimes--){
    work.push([]);
  }

  // value of matrix to add starting from top left going to
  // bottom right
  var valueOfMatrix = 1;

  for (var i = 0; i < 9; i++){
    for (var j = 0; j < 9; j++){
      work[i].push(Number(document.))
    }
  }

  console.table(work);
  debugger;
}